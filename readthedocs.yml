# readthedocs.yml
version: 2
build:
  os: ubuntu-22.04
  tools:
    python: "3.9"
  commands:
    - pip install sphinx
    - sphinx-build -b html docs/ _build/html

python:
  install:
    - method: pip
      path: .
